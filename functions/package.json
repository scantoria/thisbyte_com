{
  "name": "functions",
  "version": "1.0.0",
  "description": "Firebase Functions for ThisByte website",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "18"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^7.0.2",
    "nodemailer": "^7.0.11"
  },
  "devDependencies": {
    "@types/node": "^25.0.3",
    "@types/nodemailer": "^7.0.4",
    "typescript": "^5.9.3"
  }
}
